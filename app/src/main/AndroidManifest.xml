<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.haridas.creditpay">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <activity -->
        <!-- android:name="in.haridas.creditpay.activity.NewCardForm" -->
        <!-- android:label="@string/title_activity_new_card_form" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->


        <!-- <provider -->
        <!-- android:name="in.haridas.creditpay.contentprovider.CardContentProvider" -->
        <!-- android:authorities="in.haridas.creditpay.contentprovider" /> -->


        <!-- <activity android:name="in.haridas.creditpay.activity.CardView" -->
        <!-- android:label="Update Card" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <!--<activity android:name=".activity.WindowPreview"-->
            <!--android:theme="@style/AppTheme.NoActionBar">-->
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        <!--</activity>-->
    </application>

</manifest>